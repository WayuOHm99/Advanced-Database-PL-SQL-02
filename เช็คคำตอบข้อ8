เช็ต 8.1
BEGIN
    DEPT_REPORT(50);  -- ระบุ department_id ที่ต้องการ
END;

เช็ค 8.2
DECLARE
    v_result VARCHAR2(100);
BEGIN
    v_result := EMP_WORK_LOG(1000);  -- ระบุ employee_id ที่ต้องการ
    DBMS_OUTPUT.PUT_LINE(v_result);
END;
